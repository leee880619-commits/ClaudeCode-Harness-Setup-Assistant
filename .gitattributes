# Normalize line endings to LF in the repository, regardless of OS.
* text=auto eol=lf

# Shell scripts must be LF (otherwise shebang breaks on Linux/macOS).
*.sh text eol=lf

# Binary files (don't touch).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
